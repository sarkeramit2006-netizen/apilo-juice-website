const menu=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.getElementById('year').textContent=new Date().getFullYear();const PRICE=1000;const quantity=document.getElementById('quantity');const minus=document.getElementById('minus');const plus=document.getElementById('plus');const summary=document.getElementById('quantitySummary');const total=document.getElementById('totalSummary');const paymentTotal=document.getElementById('paymentTotal');function updateQuantity(){let value=Math.max(1,Math.min(50,parseInt(quantity.value||1,10)));quantity.value=value;if(summary)summary.textContent=`${value} ${value===1?'bottle':'bottles'}`;const amount=value*PRICE;if(total)total.textContent=`৳${amount.toLocaleString('en-BD')}`;if(paymentTotal)paymentTotal.textContent=amount.toLocaleString('en-BD')}minus?.addEventListener('click',()=>{quantity.value=Math.max(1,(parseInt(quantity.value||1,10)-1));updateQuantity()});plus?.addEventListener('click',()=>{quantity.value=Math.min(50,(parseInt(quantity.value||1,10)+1));updateQuantity()});quantity?.addEventListener('input',updateQuantity);updateQuantity();document.getElementById('orderForm')?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);const qty=parseInt(data.get('quantity'),10);const amount=qty*PRICE;const result=document.getElementById('orderResult');result.style.display='block';result.innerHTML=`Order details received: <b>${qty} bottle(s)</b> of APOLLO Mango Juice 250ml for <b>${data.get('customerName')}</b>.<br>Total payment: <b>৳${amount.toLocaleString('en-BD')}</b> via <b>bKash</b> to <b>01834331259</b>.<br>Transaction ID: <b>${data.get('transactionId')}</b>.<br><b>Note:</b> this static website does not process bKash automatically; the seller must confirm the payment and order.`;result.scrollIntoView({behavior:'smooth',block:'center'})});