const menu=document.querySelector('.menu-btn');const nav=document.querySelector('.nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.getElementById('year').textContent=new Date().getFullYear();const quantity=document.getElementById('quantity');const minus=document.getElementById('minus');const plus=document.getElementById('plus');const summary=document.getElementById('quantitySummary');function updateQuantity(){let value=Math.max(1,Math.min(50,parseInt(quantity.value||1,10)));quantity.value=value;summary.textContent=`${value} ${value===1?'bottle':'bottles'}`}minus?.addEventListener('click',()=>{quantity.value=Math.max(1,(parseInt(quantity.value||1,10)-1));updateQuantity()});plus?.addEventListener('click',()=>{quantity.value=Math.min(50,(parseInt(quantity.value||1,10)+1));updateQuantity()});quantity?.addEventListener('input',updateQuantity);document.getElementById('orderForm')?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);const result=document.getElementById('orderResult');result.style.display='block';result.innerHTML=`Order details received: <b>${data.get('quantity')} bottle(s)</b> of APOLLO Mango Juice 250ml for <b>${data.get('customerName')}</b>.<br>Payment method: <b>bKash</b> to <b>01834331259</b>. Please keep your transaction ID. <b>Note:</b> this static website does not process the payment automatically; the order still needs to be confirmed by the seller.`;result.scrollIntoView({behavior:'smooth',block:'center'})});